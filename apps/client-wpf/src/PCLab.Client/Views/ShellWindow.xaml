<Window x:Class="PCLab.Client.Views.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PC Lab Lock Overlay"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowState="Maximized"
        Topmost="True"
        ShowInTaskbar="False"
        Background="#E6171F2D"
        Loaded="Window_OnLoaded"
        Deactivated="Window_OnDeactivated">
  <Grid Margin="48">
    <Border MaxWidth="560"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="#F8FAFC"
            CornerRadius="24"
            Padding="36">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
          <TextBlock Text="School Computer Lab"
                     Foreground="#1D4ED8"
                     FontSize="16"
                     FontWeight="Bold" />
          <TextBlock Margin="0,10,0,0"
                     Text="Student check-in required"
                     Foreground="#0F172A"
                     FontSize="32"
                     FontWeight="SemiBold" />
          <TextBlock Margin="0,14,0,0"
                     Text="This shared lab PC stays locked until a student checks in. Enter the student ID below to begin the session."
                     Foreground="#334155"
                     FontSize="16"
                     TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,28,0,0">
          <TextBlock Text="Student ID"
                     FontSize="14"
                     FontWeight="SemiBold"
                     Foreground="#0F172A" />
          <TextBox Margin="0,8,0,0"
                   Padding="14"
                   FontSize="22"
                   Text="{Binding CheckIn.StudentId, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <TextBlock Grid.Row="2"
                   Margin="0,14,0,0"
                   Text="{Binding CheckIn.ErrorMessage}"
                   Foreground="#B91C1C"
                   FontSize="14"
                   TextWrapping="Wrap" />

        <TextBlock Grid.Row="3"
                   Margin="0,18,0,0"
                   Text="{Binding StatusMessage}"
                   Foreground="#475569"
                   FontSize="14"
                   TextWrapping="Wrap" />

        <Button Grid.Row="4"
                Margin="0,28,0,0"
                Height="52"
                Command="{Binding CheckIn.CheckInCommand}"
                Background="#1D4ED8"
                Foreground="White"
                FontSize="18"
                FontWeight="Bold"
                Content="Start Session" />
      </Grid>
    </Border>
  </Grid>
</Window>
